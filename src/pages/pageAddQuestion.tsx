import DefaultView from "../components/DefaultView";
import QuestionForm from "../components/QuestionForm";

const PageAddQuestion = () => {
    return (
        <DefaultView component={QuestionForm} />
    );
};

export default PageAddQuestion;