import DefaultView from "../components/DefaultView";
import MyData from "../components/MyData";

const PageMyData = () => {
    return (
        <DefaultView component={MyData}/>
    );
};

export default PageMyData;
